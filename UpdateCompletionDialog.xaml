<Window x:Class="WorkLogTool.UpdateCompletionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="更新完成度" Width="400" Height="300"
        WindowStartupLocation="CenterScreen">
    <StackPanel Margin="20">
        <TextBlock Text="选择任务:" Margin="0 0 0 5"/>
        <ComboBox x:Name="TaskComboBox" Margin="0 0 0 15"/>

        <TextBlock Text="完成度" Margin="0 0 0 5"/>
        <Slider x:Name="CompletionSlider" Minimum="0" Maximum="100" Value="0" Margin="0 0 0 15" ValueChanged="CompletionSlider_ValueChanged"/>
        <TextBlock x:Name="CompletionText" Text="0%" HorizontalAlignment="Center" Margin="0 0 0 15"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CancelButton" Content="取消" Margin="0 0 10 0" Width="80" Click="CancelButton_Click"/>
            <Button x:Name="UpdateButton" Content="更新" Width="80" Click="UpdateButton_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
